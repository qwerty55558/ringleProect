source "https://rubygems.org"

# Bundle edge Rails instead: gem "rails", github: "rails/rails", branch: "main"
gem "rails", "~> 7.2.2"
# Use sqlite3 as the database for Active Record
gem "sqlite3", ">= 1.4"
# Falcon — fiber-based async web server. Replaces Puma so each open
# SSE connection (/me/stream, /admin/memberships/stream, /ai/messages)
# parks a cheap fiber in the event loop instead of pinning a real OS
# thread. A 16-tab refresh storm becomes 16 fibers (~few KB total) and
# closed sockets are detected by the event loop in microseconds.
# See config/environments/*.rb for the matching `:fiber` isolation
# level and `async_query_executor` settings.
gem "falcon", "~> 0.51"
gem "async-http", "~> 0.86"
# Use Redis adapter to run Action Cable in production
# gem "redis", ">= 4.0.1"

# Use Kredis to get higher-level data types in Redis [https://github.com/rails/kredis]
# gem "kredis"

# Use Active Model has_secure_password [https://guides.rubyonrails.org/active_model_basics.html#securepassword]
# gem "bcrypt", "~> 3.1.7"

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
gem "tzinfo-data", platforms: %i[ windows jruby ]

# Use Active Storage variants [https://guides.rubyonrails.org/active_storage_overview.html#transforming-images]
# gem "image_processing", "~> 1.2"

# Use Rack CORS for handling Cross-Origin Resource Sharing (CORS), making cross-origin Ajax possible
gem "rack-cors"

# HTTP client for Gemini API
gem "faraday", "~> 2.9"
gem "faraday-net_http", "~> 3.1"

# Throttle/abuse protection
gem "rack-attack", "~> 6.7"

# Load env vars from .env in development/test
gem "dotenv-rails", "~> 3.1", groups: [:development, :test]

group :development, :test do
  # See https://guides.rubyonrails.org/debugging_rails_applications.html#debugging-with-the-debug-gem
  gem "debug", platforms: %i[ mri windows ], require: "debug/prelude"

  # Static analysis for security vulnerabilities [https://brakemanscanner.org/]
  gem "brakeman", require: false

  # Omakase Ruby styling [https://github.com/rails/rubocop-rails-omakase/]
  gem "rubocop-rails-omakase", require: false

  gem "rspec-rails", "~> 7.0"
  gem "factory_bot_rails", "~> 6.4"
  gem "shoulda-matchers", "~> 6.4"
  gem "webmock", "~> 3.23"
end


